import styled from "styled-components";

export default styled.header`
  &,
  nav {
    display: flex;
    justify-content: space-between;
  }

  nav {
    width: 10rem;
  }

  align-items: center;

  margin: 0 2rem;
`;
